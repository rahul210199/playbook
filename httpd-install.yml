---
- hosts: testlinux
  tasks: 
    - name: install mariaDB
      ansible.builtin.apt-get:
        name: mariaDB
        state: latest
    - name: start and enable mariaDB
      ansible.builtin.service:
        name: mariaDB
        enabled: yes
        state: started
        
